<ContentDialog x:Class="Videoficha.Features.SystemDiagnostics.Views.SystemInfoWindow"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d"
               Title="Información del Sistema"
               CloseButtonText="Cerrar">

    <ContentDialog.Resources>
        <FontFamily x:Key="CustomFontRegular">ms-appx:///Assets/Fonts/FormaDJRMicro-Regular.ttf#Forma DJR Micro</FontFamily>
        <FontFamily x:Key="CustomFontBold">ms-appx:///Assets/Fonts/FormaDJRMicro-Bold.ttf#Forma DJR Micro</FontFamily>
        
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </ContentDialog.Resources>

    <Grid Margin="10" Width="600">
        <StackPanel>
            <TextBlock Text="Información del Sistema" FontFamily="{StaticResource CustomFontBold}" FontSize="36" Foreground="#0292D8" HorizontalAlignment="Center" Margin="0,10,0,20"/>
            
            <TextBlock>
                <Run Text="Modelo: " FontFamily="{StaticResource CustomFontBold}"/>
                <Run Text="{Binding SystemSpec.Model}" FontFamily="{StaticResource CustomFontRegular}"/>
            </TextBlock>
            
            <TextBlock>
                <Run Text="Sistema Operativo: " FontFamily="{StaticResource CustomFontBold}"/>
                <Run Text="{Binding SystemSpec.OS}" FontFamily="{StaticResource CustomFontRegular}"/>
            </TextBlock>
            
            <TextBlock>
                <Run Text="Procesador: " FontFamily="{StaticResource CustomFontBold}"/>
                <Run Text="{Binding SystemSpec.Processor}" FontFamily="{StaticResource CustomFontRegular}"/>
            </TextBlock>
            
            <TextBlock>
                <Run Text="RAM: " FontFamily="{StaticResource CustomFontBold}"/>
                <Run Text="{Binding SystemSpec.RAM}" FontFamily="{StaticResource CustomFontRegular}"/>
            </TextBlock>
            
            <TextBlock>
                <Run Text="Almacenamiento: " FontFamily="{StaticResource CustomFontBold}"/>
                <Run Text="{Binding SystemSpec.Storage}" FontFamily="{StaticResource CustomFontRegular}"/>
            </TextBlock>
            
            <TextBlock>
                <Run Text="Tarjeta Gráfica: " FontFamily="{StaticResource CustomFontBold}"/>
                <Run Text="{Binding SystemSpec.Graphics}" FontFamily="{StaticResource CustomFontRegular}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</ContentDialog>
